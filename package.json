{
  "name": "gmtc56-phone-helper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:client": "cd client && npm run clean-build",
    "build:server": "cd server && npm run clean-build",
    "deploy": "npm run build:server && npm run build:client && mv client/dist server/dist/public"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "engines": {
    "npm": ">= 7.0.0"
  },
  "workspaces": [
    "./*"
  ]
}
